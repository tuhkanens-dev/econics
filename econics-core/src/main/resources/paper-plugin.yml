name: econics-core
version: '2.1.1'
main: dev.tuhkanens.econicscore.Main
api-version: '1.21'
authors: [TuhkanenS]

dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true